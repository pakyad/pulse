rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // ── TOTAL INSTITUTIONAL BYPASS ──
    // Absolute public access to confirm if rules are even being active.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
